# Core Scientific Computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# ========================================
# GPU-ACCELERATED DEEP LEARNING
# ========================================
# Para instalar PyTorch con soporte CUDA (GPU), ejecuta:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
# IMPORTANTE: NO uses "pip install -r requirements.txt" directamente
# Primero instala PyTorch con CUDA usando el comando de arriba
# Luego instala el resto: pip install -r requirements.txt
#
# Si solo tienes CPU (sin GPU), usa:
# pip install torch torchvision torchaudio
#
# Verificar instalación GPU:
# python -c "import torch; print(f'CUDA: {torch.cuda.is_available()}')"
# ========================================

# Machine Learning (se instalará con PyTorch arriba)
# torch>=2.0.0  # INSTALAR CON COMANDO CUDA DE ARRIBA
# torchvision>=0.15.0  # INSTALAR CON COMANDO CUDA DE ARRIBA
scikit-learn>=1.3.0

# Fuzzy Logic
scikit-fuzzy>=0.4.2

# Visualization & Plotting
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0
kaleido>=0.2.1

# Graph & Network Visualization
networkx>=3.1
# pygraphviz>=1.11  # Optional: Requires Graphviz system install (see docs/INSTALLATION.md)

# Image & Animation
imageio>=2.31.0
pillow>=9.5.0
imageio-ffmpeg>=0.4.8

# Terminal UI & Aesthetics
rich>=13.3.0
colorama>=0.4.6
tqdm>=4.65.0
tabulate>=0.9.0
pyfiglet>=0.8.0

# Utilities
pyyaml>=6.0
joblib>=1.3.0
python-dotenv>=1.0.0
questionary>=2.0.0
kaggle>=1.5.16
requests>=2.31.0
psutil>=5.9.0  # System resource monitoring

# Development & Testing
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.7.0
flake8>=6.1.0
mypy>=1.5.0

psutils   # System resource monitoring